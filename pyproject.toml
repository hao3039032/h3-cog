[project]
name = "h3-cog"
version = "0.1.0"
description = "MiniMax H3 REF2VA video Cog for app.nz"
requires-python = ">=3.10"
license = "MIT"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.4,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
